# AEC/VEC Election Dashboard – Python dependencies
# Install with: pip install -r requirements.txt

# HTTP requests (download pipeline)
requests>=2.31.0

# HTML parsing (Wikipedia poll scraper)
beautifulsoup4>=4.12.0

# Data processing (used in analysis scripts + later frontend API)
pandas>=2.0.0

# Excel file reading (for VEC data — pandas uses openpyxl under the hood)
openpyxl>=3.1.0

# Progress bars (nice for long downloads/loads)
tqdm>=4.66.0

# Scientific computing (used by betting_odds.py for inverse normal CDF)
scipy>=1.11.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Linting (pyflakes/pycodestyle error checks — see ruff.toml)
ruff>=0.6.0
